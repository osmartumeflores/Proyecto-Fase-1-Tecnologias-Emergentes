{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "setup-react-project",
			"type": "shell",
			"command": "npm create vite@latest tienda-virtual -- --template react && cd tienda-virtual && npm install"
		}
	]
}